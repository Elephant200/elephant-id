{
  "name": "ai-benchmark",
  "version": "0.1.0",
  "private": true,
  "description": "Static browser benchmark for local AI and computer-vision workload suitability",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name ai-benchmark"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  },
  "allowScripts": {
    "esbuild@0.25.12": true
  },
  "dependencies": {
    "onnxruntime-web": "^1.27.0"
  }
}
